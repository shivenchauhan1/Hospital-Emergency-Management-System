export const ALGORITHM_CATEGORIES = [
  { id: 'sorting', name: 'Sorting', icon: 'BarChart2', color: 'from-amber-500 to-orange-600', count: 5 },
  { id: 'graph', name: 'Graph Algorithms', icon: 'Share2', color: 'from-cyan-500 to-blue-600', count: 7 },
  { id: 'tree', name: 'Tree Algorithms', icon: 'GitMerge', color: 'from-emerald-500 to-teal-600', count: 2 },
  { id: 'dp', name: 'Dynamic Programming', icon: 'Grid', color: 'from-purple-500 to-indigo-600', count: 2 },
  { id: 'backtracking', name: 'Backtracking', icon: 'Compass', color: 'from-pink-500 to-rose-600', count: 2 },
];

export const ALGORITHMS_DATA = {
  // --- SORTING (5) ---
  bubble: {
    id: 'bubble',
    name: 'Bubble Sort',
    category: 'sorting',
    categoryName: 'Sorting',
    shortDesc: 'Repeatedly steps through the list, compares adjacent elements, and swaps them if in wrong order.',
    description: 'Bubble Sort is a simple comparison-based sorting algorithm. It repeatedly iterates through the array, comparing adjacent elements and swapping them if they are out of order. Smaller values "bubble" to the top with each pass until the entire list is sorted.',
    pseudocode: [
      'procedure bubbleSort(A : list of sortable items)',
      '    n := length(A)',
      '    repeat',
      '        swapped := false',
      '        for i := 1 to n-1 do',
      '            if A[i-1] > A[i] then',
      '                swap(A[i-1], A[i])',
      '                swapped := true',
      '            end if',
      '        end for',
      '        n := n - 1',
      '    until not swapped',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N)', average: 'O(N²)', worst: 'O(N²)' },
    spaceComplexity: 'O(1)',
    stable: true,
    inPlace: true,
    applications: ['Educational demonstrations', 'Small datasets where simplicity is preferred', 'Detecting nearly-sorted lists in O(N) time'],
    advantages: ['Simple to understand and implement', 'Requires no additional memory (O(1) space)', 'Detects sorted array efficiently with flag'],
    disadvantages: ['Incredibly inefficient O(N²) for large datasets', 'Excessive number of element swaps']
  },
  insertion: {
    id: 'insertion',
    name: 'Insertion Sort',
    category: 'sorting',
    categoryName: 'Sorting',
    shortDesc: 'Builds the final sorted array one item at a time by inserting elements into correct relative position.',
    description: 'Insertion Sort works by maintaining a sorted sub-list at the start of the array. It takes each unsorted element and shifts larger elements to the right to insert the current item into its proper sorted index.',
    pseudocode: [
      'procedure insertionSort(A : list of sortable items)',
      '    for i := 1 to length(A)-1 do',
      '        key := A[i]',
      '        j := i - 1',
      '        while j >= 0 and A[j] > key do',
      '            A[j + 1] := A[j]',
      '            j := j - 1',
      '        end while',
      '        A[j + 1] := key',
      '    end for',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N)', average: 'O(N²)', worst: 'O(N²)' },
    spaceComplexity: 'O(1)',
    stable: true,
    inPlace: true,
    applications: ['Online streaming data sorting', 'Small arrays (N < 50)', 'Used as base case in Timsort and Introsort'],
    advantages: ['Adaptive: O(N) for nearly sorted data', 'Low overhead, faster than Quick/Merge on small lists', 'Stable and in-place'],
    disadvantages: ['Quadratic O(N²) complexity for reverse sorted inputs', 'High element movement']
  },
  merge: {
    id: 'merge',
    name: 'Merge Sort',
    category: 'sorting',
    categoryName: 'Sorting',
    shortDesc: 'Divide and conquer algorithm that recursively splits array in half and merges sorted halves.',
    description: 'Merge Sort divides the input array into two halves, recursively sorts both halves, and then merges the two sorted halves into a single cohesive sorted array. It guarantees O(N log N) performance.',
    pseudocode: [
      'procedure mergeSort(A, left, right)',
      '    if left < right then',
      '        mid := (left + right) / 2',
      '        mergeSort(A, left, mid)',
      '        mergeSort(A, mid + 1, right)',
      '        merge(A, left, mid, right)',
      '    end if',
      'end procedure',
      '',
      'procedure merge(A, left, mid, right)',
      '    copy sub-arrays L and R',
      '    compare and insert back into A[left...right]',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N log N)', average: 'O(N log N)', worst: 'O(N log N)' },
    spaceComplexity: 'O(N)',
    stable: true,
    inPlace: false,
    applications: ['External sorting of large disk data', 'Linked list sorting', 'Guaranteed worst-case performance requirements'],
    advantages: ['Guaranteed O(N log N) performance regardless of input distribution', 'Stable sort preserving duplicate order', 'Parallelizes extremely well'],
    disadvantages: ['Requires O(N) auxiliary space', 'Higher constant factor than QuickSort']
  },
  quick: {
    id: 'quick',
    name: 'Quick Sort',
    category: 'sorting',
    categoryName: 'Sorting',
    shortDesc: 'Picks a pivot element and partitions array so elements smaller than pivot go left and larger go right.',
    description: 'Quick Sort is a highly efficient divide-and-conquer sorting algorithm. It selects a pivot element, partitions the array around the pivot, and recursively applies the process to the left and right sub-arrays.',
    pseudocode: [
      'procedure quickSort(A, low, high)',
      '    if low < high then',
      '        pivotIdx := partition(A, low, high)',
      '        quickSort(A, low, pivotIdx - 1)',
      '        quickSort(A, pivotIdx + 1, high)',
      '    end if',
      'end procedure',
      '',
      'procedure partition(A, low, high)',
      '    pivot := A[high]',
      '    i := low - 1',
      '    for j := low to high - 1 do',
      '        if A[j] <= pivot then',
      '            i := i + 1; swap A[i] with A[j]',
      '        end if',
      '    end for',
      '    swap A[i + 1] with A[high]',
      '    return i + 1',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N log N)', average: 'O(N log N)', worst: 'O(N²)' },
    spaceComplexity: 'O(log N)',
    stable: false,
    inPlace: true,
    applications: ['C qsort and C++ std::sort (Introsort)', 'System-level memory sorting', 'General-purpose high-speed sorting'],
    advantages: ['Fastest general-purpose sorting algorithm in practice', 'In-place with low memory footprint O(log N)', 'Excellent cache locality'],
    disadvantages: ['Worst case O(N²) when poorly pivoted', 'Unstable sort']
  },
  heap: {
    id: 'heap',
    name: 'Heap Sort',
    category: 'sorting',
    categoryName: 'Sorting',
    shortDesc: 'Converts array into a Max-Heap binary tree, then repeatedly extracts maximum element.',
    description: 'Heap Sort uses a binary heap data structure. It first constructs a Max-Heap from the array, then repeatedly extracts the root (maximum element), swapping it with the end element and re-heapifying the remaining elements.',
    pseudocode: [
      'procedure heapSort(A)',
      '    n := length(A)',
      '    for i := n/2 - 1 down to 0 do',
      '        heapify(A, n, i)',
      '    end for',
      '    for i := n - 1 down to 1 do',
      '        swap(A[0], A[i])',
      '        heapify(A, i, 0)',
      '    end for',
      'end procedure',
      '',
      'procedure heapify(A, n, i)',
      '    largest := i, left := 2i + 1, right := 2i + 2',
      '    if left < n and A[left] > A[largest] then largest := left',
      '    if right < n and A[right] > A[largest] then largest := right',
      '    if largest != i then swap A[i], A[largest]; heapify(A, n, largest)',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N log N)', average: 'O(N log N)', worst: 'O(N log N)' },
    spaceComplexity: 'O(1)',
    stable: false,
    inPlace: true,
    applications: ['Priority queue implementations', 'Embedded systems with hard real-time and strict O(1) space limits', 'Order statistics (kth largest element)'],
    advantages: ['Guaranteed O(N log N) time complexity', 'In-place sorting using O(1) memory', 'No recursion overhead'],
    disadvantages: ['Poorer cache performance than QuickSort', 'Unstable sort']
  },

  // --- GRAPH ALGORITHMS (7) ---
  bfs: {
    id: 'bfs',
    name: 'Breadth-First Search (BFS)',
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Explores graph level-by-level using a Queue data structure.',
    description: 'Breadth-First Search traverses a graph level by level starting from a source node. It uses a First-In-First-Out (FIFO) queue to visit all adjacent neighbors before proceeding to nodes at the next depth level.',
    pseudocode: [
      'procedure BFS(G, start_node)',
      '    let Q be a queue',
      '    label start_node as visited',
      '    Q.enqueue(start_node)',
      '    while Q is not empty do',
      '        v := Q.dequeue()',
      '        for all neighbors w of v in G do',
      '            if w is not visited then',
      '                label w as visited',
      '                Q.enqueue(w)',
      '            end if',
      '        end for',
      '    end while',
      'end procedure'
    ],
    timeComplexity: { best: 'O(V + E)', average: 'O(V + E)', worst: 'O(V + E)' },
    spaceComplexity: 'O(V)',
    applications: ['Shortest path in unweighted graphs', 'Web crawlers', 'Social network connections (degrees of separation)', 'GPS navigation routing'],
    advantages: ['Guarantees shortest path in unweighted graphs', 'Simple queue-based implementation', 'Finds all nodes within K steps'],
    disadvantages: ['Requires storing all frontier nodes in memory (O(V) memory space)', 'Inefficient for deep graph trees']
  },
  dfs: {
    id: 'dfs',
    name: 'Depth-First Search (DFS)',
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Explores as far as possible along each branch before backtracking using a Stack or Recursion.',
    description: 'Depth-First Search traverses a graph by exploring as deep as possible along each branch before backtracking. It uses a LIFO stack or recursive calls to explore unvisited neighbor paths.',
    pseudocode: [
      'procedure DFS(G, v)',
      '    label v as visited',
      '    for all neighbors w of v in G do',
      '        if w is not visited then',
      '            DFS(G, w)',
      '        end if',
      '    end for',
      'end procedure'
    ],
    timeComplexity: { best: 'O(V + E)', average: 'O(V + E)', worst: 'O(V + E)' },
    spaceComplexity: 'O(V)',
    applications: ['Cycle detection in graphs', 'Topological sorting', 'Solving mazes and puzzles', 'Connected components analysis'],
    advantages: ['Low memory usage for deep narrow graphs', 'Ideal for path finding and cycle checking', 'Naturally fits recursive design'],
    disadvantages: ['Does NOT guarantee shortest path', 'Can get trapped in infinitely deep paths without visited check']
  },
  dijkstra: {
    id: 'dijkstra',
    name: "Dijkstra's Algorithm",
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Finds single-source shortest paths in weighted graph with non-negative edge weights using Priority Queue.',
    description: "Dijkstra's algorithm calculates the shortest path from a starting node to all other nodes in a weighted graph with non-negative edges. It uses a greedy approach, repeatedly relaxing distances to unvisited neighbors using a min-priority queue.",
    pseudocode: [
      'procedure Dijkstra(G, source)',
      '    dist[source] := 0, for all other v: dist[v] := infinity',
      '    PQ := priority queue containing all nodes ordered by dist[]',
      '    while PQ is not empty do',
      '        u := PQ.extract_min()',
      '        for each neighbor v of u do',
      '            alt := dist[u] + weight(u, v)',
      '            if alt < dist[v] then',
      '                dist[v] := alt',
      '                prev[v] := u',
      '                PQ.decrease_key(v, alt)',
      '            end if',
      '        end for',
      '    end while',
      'end procedure'
    ],
    timeComplexity: { best: 'O((V + E) log V)', average: 'O((V + E) log V)', worst: 'O((V + E) log V)' },
    spaceComplexity: 'O(V)',
    applications: ['Google Maps / OpenStreetMap turn-by-turn navigation', 'Network routing protocols (OSPF, IS-IS)', 'Flight path optimization'],
    advantages: ['Optimal shortest path guarantee for non-negative weights', 'Very fast with binary/fibonacci heap', 'Widely deployed industry standard'],
    disadvantages: ['Fails on graphs with negative edge weights', 'Explores radially in all directions without heuristic guidance']
  },
  floyd_warshall: {
    id: 'floyd_warshall',
    name: 'Floyd–Warshall Algorithm',
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'All-pairs shortest path dynamic programming algorithm working on directed and undirected graphs.',
    description: 'Floyd–Warshall finds shortest paths between all pairs of nodes in a weighted graph. It uses dynamic programming, incrementally building a distance matrix by testing whether passing through an intermediate node k shortens distance between i and j.',
    pseudocode: [
      'procedure FloydWarshall(G)',
      '    let dist be a V x V matrix of distances initialized to infinity',
      '    for each vertex v set dist[v][v] := 0',
      '    for each edge (u, v) with weight w set dist[u][v] := w',
      '    for k from 1 to V do',
      '        for i from 1 to V do',
      '            for j from 1 to V do',
      '                if dist[i][j] > dist[i][k] + dist[k][j] then',
      '                    dist[i][j] := dist[i][k] + dist[k][j]',
      '                end if',
      '            end for',
      '        end for',
      '    end for',
      'end procedure'
    ],
    timeComplexity: { best: 'O(V³)', average: 'O(V³)', worst: 'O(V³)' },
    spaceComplexity: 'O(V²)',
    applications: ['Transitive closure computation', 'All-pairs shortest distance matrices', 'Finding negative cycles in graphs'],
    advantages: ['Calculates paths between ALL pairs of vertices', 'Extremely simple triple nested loop implementation', 'Detects negative-weight cycles'],
    disadvantages: ['Cubic time O(V³) infeasible for large graphs (V > 1000)', 'Requires O(V²) matrix storage']
  },
  prim: {
    id: 'prim',
    name: "Prim's MST Algorithm",
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Greedy algorithm that grows a Minimum Spanning Tree from a starting node by adding cheapest cut edge.',
    description: "Prim's algorithm builds a Minimum Spanning Tree (MST) for a connected weighted graph. It starts at an arbitrary node and continuously adds the minimum weight edge connecting a visited node to an unvisited node until all nodes are included.",
    pseudocode: [
      'procedure Prim(G, start)',
      '    MST := empty, visited := {start}',
      '    PQ := min priority queue of edges connected to start',
      '    while visited does not contain all nodes do',
      '        (u, v, weight) := PQ.extract_min()',
      '        if v is not in visited then',
      '            add (u, v) to MST',
      '            add v to visited',
      '            add all edges from v to unvisited nodes into PQ',
      '        end if',
      '    end while',
      'end procedure'
    ],
    timeComplexity: { best: 'O(E log V)', average: 'O(E log V)', worst: 'O(E log V)' },
    spaceComplexity: 'O(V + E)',
    applications: ['Telecommunications network cable layout', 'Electrical grid routing', 'Water pipeline supply networks'],
    advantages: ['Optimal MST for dense graphs', 'Grows single connected tree organically', 'Efficient with Fibonacci Heap'],
    disadvantages: ['Requires connected graph', 'Slower than Kruskal on sparse graphs']
  },
  kruskal: {
    id: 'kruskal',
    name: "Kruskal's MST Algorithm",
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Sorts all edges by weight and uses Union-Find (Disjoint Set) to build Minimum Spanning Forest.',
    description: "Kruskal's algorithm computes the MST by treating every vertex as an independent set. It sorts all edges in ascending weight and adds edges one by one, using a Disjoint Set Union (DSU) data structure to prevent cycle creation.",
    pseudocode: [
      'procedure Kruskal(G)',
      '    MST := empty',
      '    DSU := initialize disjoint sets for all vertices in G',
      '    sort all edges E by weight ascending',
      '    for each edge (u, v) with weight w in sorted E do',
      '        if DSU.find(u) != DSU.find(v) then',
      '            add edge (u, v) to MST',
      '            DSU.union(u, v)',
      '        end if',
      '    end for',
      'end procedure'
    ],
    timeComplexity: { best: 'O(E log E)', average: 'O(E log E)', worst: 'O(E log E)' },
    spaceComplexity: 'O(V + E)',
    applications: ['Cluster analysis & image segmentation', 'Network layout design', 'Approximation algorithms for Travelling Salesperson Problem'],
    advantages: ['Works efficiently on sparse graphs', 'Handles disconnected components (Spanning Forest)', 'Simple edge-centric sorting logic'],
    disadvantages: ['Sorting step overhead on dense graphs', 'Requires DSU data structure tracking']
  },
  bellman_ford: {
    id: 'bellman_ford',
    name: 'Bellman–Ford Algorithm',
    category: 'graph',
    categoryName: 'Graph Algorithms',
    shortDesc: 'Computes single-source shortest paths and detects negative weight cycles by relaxing edges V-1 times.',
    description: 'Bellman–Ford computes single-source shortest paths on weighted directed graphs. Unlike Dijkstra, it handles negative edge weights and detects negative-weight cycles by relaxing all edges |V| - 1 times.',
    pseudocode: [
      'procedure BellmanFord(G, source)',
      '    dist[source] := 0, all other dist[v] := infinity',
      '    for i := 1 to |V| - 1 do',
      '        for each edge (u, v) with weight w do',
      '            if dist[u] + w < dist[v] then',
      '                dist[v] := dist[u] + w',
      '            end if',
      '        end for',
      '    end for',
      '    for each edge (u, v) with weight w do',
      '        if dist[u] + w < dist[v] then',
      '            return "Graph contains negative-weight cycle"',
      '        end if',
      '    end for',
      'end procedure'
    ],
    timeComplexity: { best: 'O(E)', average: 'O(V * E)', worst: 'O(V * E)' },
    spaceComplexity: 'O(V)',
    applications: ['Distance-Vector routing protocols (RIP)', 'Financial arbitrage detection in currency markets', 'Graphs with negative cost transactions'],
    advantages: ['Supports negative edge weights', 'Detects negative cycles in graph', 'Distributed execution friendly'],
    disadvantages: ['Slower O(V * E) time than Dijkstra O((V+E) log V)', 'Repeats edge checks redundantly']
  },

  // --- TREE ALGORITHMS (2) ---
  bst: {
    id: 'bst',
    name: 'BST Traversals & Operations',
    category: 'tree',
    categoryName: 'Tree Algorithms',
    shortDesc: 'Binary Search Tree insertion, deletion, and In-Order, Pre-Order, Post-Order, and Level-Order traversals.',
    description: 'A Binary Search Tree (BST) maintains node keys where left subtrees contain smaller values and right subtrees contain larger values. Supports search, insertion, and classic tree traversals (In-order, Pre-order, Post-order, Level-order).',
    pseudocode: [
      'procedure inOrder(node)',
      '    if node is not null then',
      '        inOrder(node.left)',
      '        visit(node)',
      '        inOrder(node.right)',
      '    end if',
      'end procedure',
      '',
      'procedure insert(node, key)',
      '    if node is null then return new Node(key)',
      '    if key < node.key then node.left := insert(node.left, key)',
      '    else node.right := insert(node.right, key)',
      '    return node',
      'end procedure'
    ],
    timeComplexity: { best: 'O(log N)', average: 'O(log N)', worst: 'O(N)' },
    spaceComplexity: 'O(N)',
    applications: ['Dynamic sets and lookup tables', 'Expression trees in compilers', 'Hierarchical data structures'],
    advantages: ['In-order traversal yields sorted sequence in O(N)', 'Fast average search and insert O(log N)', 'Dynamic size allocation'],
    disadvantages: ['Can degenerate into O(N) linked list if skewed', 'No automatic self-balancing']
  },
  avl: {
    id: 'avl',
    name: 'AVL Tree (Self-Balancing)',
    category: 'tree',
    categoryName: 'Tree Algorithms',
    shortDesc: 'Self-balancing BST maintaining height difference (Balance Factor) <= 1 via LL, RR, LR, RL rotations.',
    description: 'An AVL tree is a self-balancing binary search tree. The heights of child subtrees for any node differ by at most 1. Rebalancing is triggered after insertions/deletions using Single (LL, RR) or Double (LR, RL) rotations.',
    pseudocode: [
      'procedure insertAVL(node, key)',
      '    standard BST insert key into node',
      '    update height of node',
      '    balance := getBalanceFactor(node)',
      '    if balance > 1 and key < node.left.key then return rotateRight(node) // LL',
      '    if balance < -1 and key > node.right.key then return rotateLeft(node) // RR',
      '    if balance > 1 and key > node.left.key then node.left := rotateLeft(node.left); return rotateRight(node) // LR',
      '    if balance < -1 and key < node.right.key then node.right := rotateRight(node.right); return rotateLeft(node) // RL',
      '    return node',
      'end procedure'
    ],
    timeComplexity: { best: 'O(log N)', average: 'O(log N)', worst: 'O(log N)' },
    spaceComplexity: 'O(N)',
    applications: ['High-lookup database indexing', 'Memory allocation tree structures', 'Real-time lookups demanding strict logarithmic bound'],
    advantages: ['Guaranteed worst-case O(log N) operations', 'Stricter balance factor than Red-Black trees', 'Faster lookups than Red-Black trees'],
    disadvantages: ['Frequent rotations during insertions and deletions', 'Requires storing balance factor/height at each node']
  },

  // --- DYNAMIC PROGRAMMING (2) ---
  knapsack: {
    id: 'knapsack',
    name: '0/1 Knapsack Problem',
    category: 'dp',
    categoryName: 'Dynamic Programming',
    shortDesc: 'Finds optimal item combination to maximize total value without exceeding capacity limit W.',
    description: 'The 0/1 Knapsack Problem determines the maximum value subset of N items with given weights and values that can fit inside a knapsack of capacity W. Uses a 2D DP matrix table K[i][w] to build overlapping subproblem solutions.',
    pseudocode: [
      'procedure 01Knapsack(weights, values, N, W)',
      '    let K[0..N][0..W] be a matrix initialized to 0',
      '    for i := 1 to N do',
      '        for w := 1 to W do',
      '            if weights[i-1] <= w then',
      '                K[i][w] := max(values[i-1] + K[i-1][w - weights[i-1]], K[i-1][w])',
      '            else',
      '                K[i][w] := K[i-1][w]',
      '            end if',
      '        end for',
      '    end for',
      '    return K[N][W]',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N * W)', average: 'O(N * W)', worst: 'O(N * W)' },
    spaceComplexity: 'O(N * W)',
    applications: ['Resource allocation in logistics and budgeting', 'Cargo loading optimization', 'Portfolio asset selection', 'Cryptographic knapsack systems'],
    advantages: ['Guarantees globally optimal solution', 'Avoids exponential recursion using DP table', 'Enables easy item reconstruction backtrack'],
    disadvantages: ['Pseudo-polynomial complexity dependent on capacity W', 'Requires matrix memory O(N * W)']
  },
  lcs: {
    id: 'lcs',
    name: 'Longest Common Subsequence (LCS)',
    category: 'dp',
    categoryName: 'Dynamic Programming',
    shortDesc: 'Finds longest subsequence present in two sequence strings in same relative order.',
    description: 'LCS computes the longest sequence that appears as a subsequence of two input strings X and Y. It constructs a 2D matrix L[i][j] where cell value represents LCS length of prefixes X[0..i-1] and Y[0..j-1].',
    pseudocode: [
      'procedure LCS(X, Y)',
      '    m := length(X), n := length(Y)',
      '    let L[0..m][0..n] be a matrix initialized to 0',
      '    for i := 1 to m do',
      '        for j := 1 to n do',
      '            if X[i-1] == Y[j-1] then',
      '                L[i][j] := L[i-1][j-1] + 1',
      '            else',
      '                L[i][j] := max(L[i-1][j], L[i][j-1])',
      '            end if',
      '        end for',
      '    end for',
      '    return L[m][n]',
      'end procedure'
    ],
    timeComplexity: { best: 'O(M * N)', average: 'O(M * N)', worst: 'O(M * N)' },
    spaceComplexity: 'O(M * N)',
    applications: ['Git diff and version control comparison', 'DNA sequence similarity analysis (Bioinformatics)', 'Plagiarism detection software'],
    advantages: ['Provides exact similarity match length and alignment', 'Solves combinatorial string alignment efficiently', 'Used heavily in diff tools'],
    disadvantages: ['O(M * N) memory quadratic for huge sequences', 'Does not account for gaps/penalties directly without modification']
  },

  // --- BACKTRACKING (2) ---
  sudoku: {
    id: 'sudoku',
    name: 'Sudoku Solver',
    category: 'backtracking',
    categoryName: 'Backtracking',
    shortDesc: 'Fills a grid with digits so each row, column, and sub-grid contains all digits without repetition.',
    description: 'The Sudoku Solver uses backtracking depth-first search. It iterates through unassigned grid cells, trying digits 1 to N, checking validity against row, column, and subgrid rules, and backtracks when a candidate leads to a dead end.',
    pseudocode: [
      'procedure solveSudoku(board)',
      '    for row := 0 to N-1 do',
      '        for col := 0 to N-1 do',
      '            if board[row][col] == 0 then',
      '                for num := 1 to N do',
      '                    if isValid(board, row, col, num) then',
      '                        board[row][col] := num',
      '                        if solveSudoku(board) then return true',
      '                        board[row][col] := 0 // Backtrack',
      '                    end if',
      '                end for',
      '                return false // Trigger backtrack',
      '            end if',
      '        end for',
      '    end for',
      '    return true',
      'end procedure'
    ],
    timeComplexity: { best: 'O(1)', average: 'O(9^(N*N))', worst: 'O(9^(N*N))' },
    spaceComplexity: 'O(N * N)',
    applications: ['Automated puzzle resolution', 'Constraint Satisfaction Problem (CSP) benchmark', 'Circuit placement optimization'],
    advantages: ['Guaranteed to find solution if one exists', 'Prunes search space aggressively with constraint checks', 'Classic backtracking template'],
    disadvantages: ['Exponential worst-case runtime without advanced heuristics', 'Can explore millions of dead-end states on hard puzzles']
  },
  rat_in_maze: {
    id: 'rat_in_maze',
    name: 'Rat in a Maze',
    category: 'backtracking',
    categoryName: 'Backtracking',
    shortDesc: 'Finds a path for a rat from top-left (0,0) to bottom-right (N-1, N-1) through obstacles.',
    description: 'Rat in a Maze uses recursive backtracking to navigate a 2D matrix of open cells (1) and blocked walls (0). The algorithm tries moving Down, Right, Up, and Left, backtracking when hitting a wall or dead-end.',
    pseudocode: [
      'procedure solveMaze(maze, x, y, solution)',
      '    if x == N-1 and y == N-1 and maze[x][y] == 1 then',
      '        solution[x][y] := 1; return true',
      '    end if',
      '    if isSafe(maze, x, y) then',
      '        solution[x][y] := 1',
      '        if solveMaze(maze, x+1, y, solution) then return true // Down',
      '        if solveMaze(maze, x, y+1, solution) then return true // Right',
      '        if solveMaze(maze, x-1, y, solution) then return true // Up',
      '        if solveMaze(maze, x, y-1, solution) then return true // Left',
      '        solution[x][y] := 0 // Backtrack',
      '        return false',
      '    end if',
      '    return false',
      'end procedure'
    ],
    timeComplexity: { best: 'O(N * N)', average: 'O(4^(N*N))', worst: 'O(4^(N*N))' },
    spaceComplexity: 'O(N * N)',
    applications: ['Robot path planning in grid maps', 'Maze generation & solving algorithms', 'Game character AI pathfinding'],
    advantages: ['Visually intuitive path discovery and backtracking', 'Easy to add diagonal moves or step weights', 'Systematically visits all valid avenues'],
    disadvantages: ['Exponential worst case on open mazes without visited grid memory', 'Not as fast as A* or BFS for shortest path']
  }
};
